export interface ThemeType {
  primary: string;
  background: string;
  text?: string;
  accent?: string;
}

export type CampaignType = 'DEFAULT' | 'BACK_TO_SCHOOL' | 'SUMMER_PLAYHOUSE' | 'MYSTERY_GIFT_CARNIVAL';
